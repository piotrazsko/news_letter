import {StyleSheet} from 'react-native';

import {hp, fs, wp} from 'src/helpers';

const styles = StyleSheet.create({
  container: {
    width: wp(100),
    height: hp(100),
  },
});

export default styles;
